format shorte
format compact

for iteration = 1:5

    fprintf('Example %d\n', iteration)

    ttformat_dict,

    %A(iteration,:) = [angle_1, angle_2, angle_3];
    %B(iteration,:) = [dist_1, dist_2, dist_3];
end

